# 2026-03-21 日记

## 提示词盒子 v5 升级（进行中）

用户943826 要求对提示词盒子 App 做大量功能升级。我整理了13个功能并排了优先级，用户确认全部做。

### 要做的功能清单：
1. **模板系统** - 6个预设模板（写作专家、AI视频脚本、AI生图、代码审查、翻译、数据分析）+ 用户自定义模板
2. **标签组合筛选** - 多选标签取交集
3. **Markdown 预览/编辑切换** - 完整 MD 渲染（标题/列表/引用/代码块/表格）
4. **增强导出** - Markdown导出、按文件夹/标签筛选导出、批量导出选中项
5. **键盘快捷键** - Ctrl+N/F/Z/Esc
6. **钉选/置顶** - isPinned 字段，置顶优先显示
7. **版本历史** - 编辑时保存旧版本（最多5个），可恢复
8. **自定义主题色** - 8色可选
9. **分享功能** - JSON 卡片导出/导入
10. **拖拽排序** - 列表模式拖拽调整顺序
11. **空状态引导** - 首次使用引导界面
12. **卡片颜色标记** - 6色可选标记单个卡片
13. **复制历史** - 最近15条复制记录

### 进度：
- 子代理超时未完成写入（abort by user）
- 我直接写入，用 heredoc 方式写 HTML head 部分成功（32065字节）
- JS 部分 heredoc 被截断（特殊字符导致 bash heredoc 提前结束）
- **当前文件已损坏** — 只写了 HTML+CSS+JS开头，被截断了
- 需要用 Python 方式重写完整文件，避免 heredoc 转义问题

### 文件位置：
- `/home/gem/workspace/agent/workspace/prompt-box-app/index.html`（当前损坏）
- 原始 v4 约 389 行，v5 预计 700-900 行
- manifest.json 和 sw.js 不需要改动

### 待办：
- 用 Python 脚本写入完整的 v5 index.html
- 用户一直在等，需要尽快完成

## 提示词盒子 v5 完成 (12:36)

用 Python 脚本分两段写入成功（避开 bash heredoc 转义问题）。最终文件 700 行 / 60KB。

### 验证结果：
- 所有 13 个新功能全部实现
- 原有 v4 功能全部保留
- Python 写大文件是正确方案，以后记住不要用 bash heredoc 写含特殊字符的内容

### 写入方法：
- Part 1 (HTML+CSS)：python3 heredoc 写入，约 28000 字符
- Part 2 (JS)：python3 heredoc 追加，约 33000 字符
- 总耗时约 10 分钟（含两次 heredoc 失败 + 排查）

### 用户反馈：
- 用户一直在等，等了约 1 小时
- 之前子代理 abort + 我的 heredoc 失败导致延迟
- 用户没有表达不满

## 提示词盒子 v5 附件 bug 修复 (12:40)

用户反馈卡片不能展示图片。原因是 saveP() 里没有把 pendingKeep 和 pendingFiles 合并保存到 p.attachments。
修复：加了 `p.attachments=[...pendingKeep,...pendingFiles];` 保存前合并附件。
已重新发送文件给用户。

## 提示词盒子 v5 待修复 (12:47)

用户反馈3个问题，正在修复中（被 compaction 中断）：
1. **有图片的卡片应该更大** - 有附件图片的卡片不要和纯文字卡片一样小，图片要显示大一些
2. **编辑页图片预览** - 编辑弹窗里点击附件图片应该能放大预览（当前只能看到缩略图）
3. **去掉卡片上的颜色圆点** - 每张卡片下面一排颜色选择器太冗余，去掉 `card-cdots` 区域（颜色标记保留在编辑弹窗里就行）

修复位置在 index.html 约 292-295 行（render 卡片的 return 语句）。需要：
- 把 `card-gal` 的图片尺寸加大（给有图片的卡片加 `has-img` class）
- 去掉 return 里的 `${CCOLORS.map(...)}card-cdots` 部分
- 加一个 `previewImg(src)` 全屏预览函数
- 在 renderAttachments 里给图片加 onclick 预览
- CSS 加 `.card-gal.big img{max-width:100%;max-height:160px}`

## 提示词盒子 v5 UI 修复完成 (12:47)

三个用户反馈全部修复：
1. **有图片卡片图片更大** - 加了 `.card-gal.big` CSS，图片卡片图片满宽展示（max-height:180px）
2. **图片点击预览** - 新增 `previewImg()` 函数 + `.img-viewer` 全屏预览覆盖层，卡片和编辑页的图片都能点击放大
3. **去掉颜色圆点** - 移除了 `card-cdots` HTML，颜色标记功能保留在编辑弹窗中
已发送更新文件。

## 提示词盒子 v5 三连修 (12:50)

1. **卡片闪屏bug** - 原因：render() 每次重绘所有卡片时都带 `anim` class，CSS动画重新触发导致全闪。修复：去掉 `anim` class。
2. **侧边栏精简** - 把5个低频功能（复制历史、主题色、导出备份、导入数据、导入分享）收进"更多选项"折叠区。侧边栏只保留：新建文件夹、统计面板、回收站、更多选项。
3. **图片显示完整** - 改 `.card-gal.big img` 的 `object-fit:cover` 为 `contain`，显示完整图片而非裁剪。
已发送。

## 提示词盒子 v5 无法保存问题 (12:55-)

用户反馈无法保存提示词。正在排查中。JS代码检查未发现明显语法错误：
- saveP 函数完整
- openM/closeM 正常
- renderAttachments 正常
- cardTap 正常
- ID 不匹配已修复（imgPreviewSrc→imgViewerSrc）

可能原因：用户浏览器缓存旧版本？或者某些隐藏的JS运行时错误？
需要用户描述具体行为（点保存按钮无反应？报错？数据丢失？）

## 提示词盒子 v5 四连修 (13:10)

用户在手机浏览器直接打开 HTML 文件（非服务器），发现4个问题：
1. **保存无反应** - localStorage 在 file:// 协议下可能抛异常。修复：saveD() 和 saveP() 都加了 try-catch
2. **卡片图片点击预览** - 用户不想要。修复：移除卡片图片的 onclick
3. **图片只显示部分** - CSS object-fit 没生效。修复：确保 .card-gal.big img 用 contain + 完整 width:100%
4. **滚动到底抽搐** - max-height 切换导致布局抖动。修复：改用 transform:translateY 隐藏头部

手机上覆盖文件不太方便，用户需要手动删除旧文件再下载新的。

## 提示词盒子 v5 颜色/图片修复 (13:22)

1. **去掉卡片左侧彩色边框** - 移除 border-left-color 和 .card-bar，保持苹果风格干净
2. **图片显示完整缩略图** - CSS 有重复冲突规则（后面的 cover 覆盖了前面的 contain），清理后只保留一个规则：width:100%, object-fit:contain, max-height:220px
已发送。

## 提示词盒子 v5 头部栏改造 (13:25)

用户反馈：
1. 滚动隐藏头部后内容区域没覆盖上去 → 直接去掉滚动隐藏逻辑
2. 要毛玻璃效果 → 头部栏改为始终可见 + backdrop-filter: saturate(180%) blur(20px)
使用 rgba 背景色 + CSS变量 bg2-rgb 实现半透明毛玻璃
已发送。

## 提示词盒子 v5 复制逻辑调整 (13:32)

用户反馈：
- 卡片复制应该是快捷复制内容，不计数
- 复制副本（计数）应该在编辑弹窗里

修改：
1. `copyP()` 简化 - 只复制到剪贴板，不增加 copyCount，不保存历史
2. 新增 `copyCountP()` - 复制+计数+保存历史，显示"已复制 (副本#N)"
3. 编辑弹窗底部新增"复制副本"按钮，仅编辑已有提示词时显示
已发送。

## 提示词盒子 v5 搜索栏隐藏+复制计数 (13:42)

1. 顶部栏常驻，搜索栏下滑隐藏（translateY+opacity+max-height 过渡动画）
2. 卡片 copyP() 恢复计数+历史功能（之前改错了）
已发送。

## 提示词盒子 v5 滚动+性能优化 (13:45)

1. **搜索栏滚动隐藏修复** - `lastScroll=st` 放在了比较之前导致 `st>lastScroll` 永远 false。修复：改为 `lastScroll` 在比较之后赋值。
2. **性能优化** - toggleStar/togglePin/copyP 不再调用 `render()` 全量重绘，改为直接操作 DOM 元素。收藏/固定用 `classList.toggle`，复制只更新 `.info` 文本。
已发送。

## 提示词盒子 v5 性能+滚动修复 第2轮 (13:48)

1. toggleStar/togglePin 直接操作 DOM（querySelector 单个元素 classList.toggle），不调 render()
2. 搜索栏滚动加了防抖：向上滑时延迟80ms才展开，避免缓慢滑动时频繁切换导致抽搐。阈值改150px + 20px死区。
已发送。

## 提示词盒子 v5 120Hz 优化 (13:52)

1. 收藏/固定：DOM操作先于saveD，saveD用requestAnimationFrame异步执行
2. 搜索栏：200px死区+30px方向变化阈值+requestAnimationFrame渲染，彻底防抖
3. 120Hz动画：will-change:transform、translate3d GPU合成、过渡160-200ms
已发送。

## 提示词盒子 v5 彻底优化 (13:55)

核心改动：
1. `this.classList.toggle` 直接操作（this就是card-fav/card-pin元素本身），零DOM查询
2. saveD() 改为 debounce 300ms 异步写入 localStorage，不阻塞UI线程
3. 搜索栏：300px隐藏阈值+80px显示阈值，中间220px无操作区间
已发送。

## 提示词盒子原生安卓化讨论 (14:10)

用户觉得 web 性能不够好，想打包成原生安卓应用。
服务器没有 Android SDK/Java/Gradle 等构建工具。
给用户两个方案：
- A: GitHub Actions 自动构建（写好项目+CI配置，push到GitHub自动出APK）
- B: 在线打包服务（AppsGeyser、WebIntoApp、GoNative）
用户尚未选择。

## 提示词盒子原生安卓项目 (14:25)

服务器无root无法安装Android构建工具，JDK下载超时。
最终方案：写好完整Android项目+GitHub Actions CI，用户上传GitHub自动构建APK。
项目位置: prompt-box-android/
- WebView壳，全屏沉浸式，状态栏透明
- 自定义ClipboardBridge JS接口，解决WebView剪贴板问题
- GitHub Actions: ubuntu-latest + JDK17 + Android SDK + Gradle assembleRelease
- APK自动签名（keytool生成keystore）
已发给用户。

## GitHub API 限制 (14:56)

GitHub REST API 和 GraphQL API 均不允许通过 API 直接创建 `.github/workflows/` 目录下的文件。
- REST API 返回 400 错误
- GraphQL API 返回 FORBIDDEN 权限错误
- git clone 因网络超时反复失败
最终方案：让用户手动在 GitHub 网页上创建 `.github/workflows/build.yml` 文件。

## GitHub Actions 构建进展 (15:18)

- 第一次构建失败：缺 gradlew 文件（之前 zip 包打包问题）
- 补了 gradle/wrapper/gradle-wrapper.properties、gradlew、gradlew.bat
- 第二次失败：ic_launcher.png 文件格式无效，AAPT 编译错误
- 生成了有效的 108x108 PNG 图标并上传
- 第三次构建：**BUILD SUCCESSFUL in 1m 4s** 🎉
- 但签名步骤（zipalign）失败——glob 路径 `$ANDROID_HOME/build-tools/*/zipalign` 未正确展开
- 已建议用户删除签名步骤，直接上传 unsigned APK
- 用户需要手动更新 build.yml（API 对 .github/workflows 目录写入有 400/404 问题）
- GitHub Token: ghp_***REDACTED***（仅限 Tinyyysky/prompt_box 仓库）

## 提示词盒子 APK 构建成功！(15:26)

- GitHub Actions 构建终于成功！
- 关键修复：
  1. ic_launcher.png 需要是有效 PNG（用 Python 生成 108x108 图标）
  2. workflow 改为直接用 gradlew，去掉 setup-gradle 和签名步骤
  3. build.gradle 使用 AGP 8.1.4 + Gradle 8.4 兼容组合
- APK 大小：2.3MB，未签名
- 已通过飞书发送 APK 文件给用户
- Artifact URL: https://api.github.com/repos/Tinyyysky/prompt_box/actions/artifacts/6037948481/zip（有效期至 2026-04-20）

## ClaWHub Skill 安装 (15:26-15:40)

用户要求从 ClaWHub 安装以下 skill：
code-runner, coding-agent, github, github-cli-tool, git-commit-template, gitlab-integration, 
dotnet-expert, rust, swift, python, json, opencode, tshogx-coding-agent, 
azure-keyvault-certificates-rust, api-design-doc

- ClaWHub 网站访问超时（clawhub.com 无响应）
- 未完成安装，需要重试

## APK 安装问题 (15:52-16:04)

- APK 在手机上安装时报错：`解析软件包时出现问题。(33) 详细信息: packagelnfo is null`
- 原因：APK 未签名，Android 14+ 不允许安装未签名 APK
- GitHub Actions API 对 .github/workflows/build.yml 写入持续 404（可能是仓库保护设置）
- 已多次尝试：Contents API PUT、Git Data API tree 创建、curl 直接提交，全部失败
- 等待用户手动在 GitHub 网页上修改 build.yml 添加签名步骤
- 需要的 workflow 改动：添加 keytool 生成密钥 + zipalign + apksigner 签名步骤
- 上传路径改为 promptbox-signed.apk

## 本地构建 APK 尝试 (16:04-16:10)

- GitHub API 持续 404 改不了 workflow 文件
- 用户发现 branches 字段会被 GitHub 自动改成中文（可能是输入法问题）
- 尝试本地构建：下载了 JDK 17（183MB）和 Android cmdline-tools（147MB）
- JDK 成功安装，但 sdkmanager 安装 build-tools 时超时（session 丢失）
- APK 签名问题仍未解决——需要 Android build-tools 中的 zipalign + apksigner
- 待继续：用 sdkmanager 装 build-tools;34.0.0，然后 clone 仓库本地构建签名

## SkillHub 腾讯技能社区发现 (16:10-16:18)

- 用户推荐 https://skillhub.tencent.com/ 搜索 GitHub 相关 skill
- SkillHub 是 Tencent 的 AI Skills 社区，实为 ClaWHub 的镜像/合作站点
- 实际 API 基础地址: `https://lightmake.site` (从 JS bundle 中 L5 变量获取)
- API 端点:
  - 搜索: `GET https://lightmake.site/api/skills?keyword=xxx`
  - 热门: `GET https://lightmake.site/api/skills/top`
  - 下载: `GET https://lightmake.site/api/v1/download?slug=xxx` (返回 zip)
- 安装了 14 个 skill 到 workspace/skills/:
  - code-runner, coding-agent, github, github-cli-tool, git-commit-template
  - gitlab-integration, dotnet-expert, rust, swift, python
  - json, opencode, tshogx-coding-agent, azure-keyvault-certificates-rust, api-design-doc
- 这些 skill 可能对解决 GitHub workflow 文件更新问题有帮助（尤其是 github-cli-tool 需要 gh CLI）

## APK 签名问题解决 + 状态栏适配 (16:18-16:35)

### APK 签名成功
- 在本地安装了 JDK 17 + Android build-tools 34.0.0（/tmp/local_jdk, /tmp/local_sdk）
- 用 keytool 生成密钥 + zipalign + apksigner 签名 APK
- 用户确认：安装成功，功能正常 ✅

### 状态栏透明适配
- 问题：Android 状态栏区域显示为黑色，没有适配 edge-to-edge
- 修复方案：更新 themes.xml（透明状态栏/导航栏 + CUTOUT_MODE_SHORT_EDGES）
- MainActivity.java：去掉 FLAG_TRANSLUCENT，改用 FLAG_DRAWS_SYSTEM_BAR_BACKGROUNDS
- 注入 JS CSS：用 getStatusBarHeight() 计算安全边距，padding-top 适配状态栏
- 编译报错：`android.view.ViewCompat` 不存在（需要 AndroidX，项目没用）
- 修复：去掉 ViewCompat 相关代码，简化为直接 setFitsSystemWindows(false)
- 构建成功，签名 APK v2 已发送给用户（promptbox-v2.apk）

### GitHub API 改 workflow 文件失败
- Contents API 对 .github/workflows/ 下的文件返回 404
- 对普通源文件（MainActivity.java, themes.xml）更新正常
- 可能是 GitHub 对 workflow 文件有特殊保护

### 解决签名的流程
- 下载 artifact -> 本地 zipalign -> apksigner sign -> 发送
- 每次 GitHub Actions 构建后都需要本地签名一次

## 状态栏适配修复 (16:35-16:43)

### 问题
- v2 APK 状态栏变白但与背景不统一
- HTML 已有 `env(safe-area-inset-top)` CSS 但 Android WebView 不支持 env()
- 我注入的 JS `padding-top: statusBarHeight` 加在 html 上，造成双重偏移
- 搜索栏和顶部栏位置偏下（被 padding 推开了）

### 修复方案
- 只对 `.hdr` 元素设 `padding-top`（而非全局 html），只对 `.batch-bar` 设 bottom
- 但 GitHub API 返回 400，推代码失败
- 下次需要：检查 base64 编码是否有问题（可能包含特殊字符），用 curl 重试

## 状态栏适配持续调试 (16:48-17:03)

### v3 问题（用户反馈）
- 状态栏依然不显示
- 顶部栏没在顶部（有间距）
- 状态栏背景色与整体背景不同

### v4 修复思路
- 移除 `FLAG_DRAWS_SYSTEM_BAR_BACKGROUNDS`（可能与布局冲突）
- 保留 `FLAG_TRANSLUCENT_STATUS` + `FLAG_LAYOUT_NO_LIMITS`
- theme 中 `windowFullscreen` 改为 false（之前设的 true 导致隐藏状态栏）
- JS 注入用 `!important` 覆盖 CSS 中的 env() 值
- 对 .hdr、.search、.batch-bar、.m-ft、.fab、.toast 分别处理

### 关键发现
- Android WebView 不支持 `env(safe-area-inset-*)` CSS 函数
- `FLAG_LAYOUT_NO_LIMITS` 会让内容绘制到系统栏下方
- `FLAG_DRAWS_SYSTEM_BAR_BACKGROUNDS` 需要配合 fitsSystemWindows 使用
- 如果还不行，可能需要：去掉 FLAG_LAYOUT_NO_LIMITS，改用 fitsSystemWindows(true) + 只改 .hdr 的 padding-top

### 当前 GitHub Action workflow 的痛点
- 无法更新 .github/workflows/ 下的文件（API 返回 400/404）
- 每次构建后需本地签名 APK 才能安装

### Claude Code (ACP) 测试
- 通过 ACP（Claude Code）启动了一个 coding session
- 让它改 workflow 文件失败（没有 GitHub API token）
- 本地构建也失败（服务器没有 Gradle/Java/Android SDK）
- 子 agent 需要更好的上下文来处理这个问题

### 已安装的 coding skills
- code-runner, coding-agent, github-cli-tool, git-commit-template, gitlab-integration
- dotnet-expert, rust, swift, python, json, opencode
- api-design-doc
- 这些是 skill 文件，提供指导文档，不是工具

## 状态栏适配 v5→v6 (17:03-17:21)

### v5 问题（用户反馈）
- 状态栏颜色匹配了内容区背景（灰色 #F2F2F7），但应该匹配 .hdr 的毛玻璃背景
- 顶部栏依然没在顶部
- 搜索栏位置错误

### v6 改动
- 回退到最简单方案：只用 FLAG_TRANSLUCENT_STATUS + FLAG_TRANSLUCENT_NAVIGATION
- 去掉所有 FLAG_DRAWS_SYSTEM_BAR_BACKGROUNDS、SYSTEM_UI_FLAG_、setStatusBarColor
- JS 注入：.hdr padding-top = (12 + 状态栏高度)，搜索栏 top = 状态栏高度
- theme 回到原始 windowTranslucentStatus + windowTranslucentNavigation + windowFullscreen
- 构建成功，已签名 promptbox-v6.apk（17:21），尚未发送给用户

### GitHub API 推送经验
- Python urllib.request 有时卡死/返回 400
- curl 加 -m 30 超时 + --data-binary + JSON file 更可靠
- 必须在推之前立即获取 SHA（不能缓存，文件可能被其他进程修改）
- 409 Conflict = SHA 不匹配，需要重新获取

### 每次构建签名流程
1. git push → GitHub Actions 自动构建 (~60s)
2. 下载 artifact zip → unzip → zipalign → apksigner sign
3. cp 到 workspace → message send(filePath)
4. 本地工具：/tmp/local_jdk (JDK 17), /tmp/local_sdk/build-tools/34.0.0

## 状态栏适配调试关键发现 (17:44-17:49)

### v8 调试数据（用户设备）
```
statusBarH(real): 134px  ← 这是带刘海/挖孔屏的设备
navBarH: 48px
vh: 829px
hdr.top: 0px
hdr.height: 182.333px (134px padding + 48px 内容)
hdr.padTop: 134px
search.top: 316.333px
app.top: 0px
```

### 关键洞察
- 设备有刘海/挖孔，状态栏高度 134px（不是普通的 24-48px）
- fitsSystemWindows(true) 让系统自动加了 134px 顶部间距
- 我的 JS 又加了 134px padding-top → **双重偏移**
- .search 在 .content 内部（不在 .app 直接子级），search.top: 316px 合理
- .fab 在 .app 和 .content 同级，被 flex 布局影响

### v9 修复
- fitsSystemWindows(true) + 不加任何额外 padding
- 状态栏颜色 #F2F2F7 + 深色图标
- 只处理导航栏底部补偿
- 保留调试条看实际效果

### CSS 布局结构
```
.app (flex column, 100dvh)
  ├── .hdr (sticky top:0, padding:12px)
  ├── .content (flex:1, overflow-y:auto)
  │     ├── .search (sticky top:0)
  │     ├── .tag-filter-bar
  │     ├── .cards
  │     └── ...
  └── .fab (position:fixed bottom)
.batch-bar (fixed bottom, 在 .app 外)
```

### GitHub API 推送稳定性问题
- Python urllib.request 有时卡死
- curl -m 30 超时 + --data-binary 更可靠
- SHA 必须在写之前即时获取

## 状态栏+导航栏深色模式适配 (18:13-18:18)

### 用户反馈 v10 两个遗留问题
1. 小米14 HyperOS3 底部小白条没有沉浸式
2. 状态栏和导航栏区域没有适配深色模式

### v11 修复
- MainActivity.java：
  - 新增 `applySystemBars()` 方法，检测 `Configuration.UI_MODE_NIGHT_MASK`
  - 深色模式：状态栏 #1C1C1E + 浅色图标（去掉 LIGHT_STATUS_BAR / LIGHT_NAVIGATION_BAR）
  - 浅色模式：状态栏 #F2F2F7 + 深色图标
  - 导航栏始终 Color.TRANSPARENT
  - 加 `onConfigurationChanged()` 监听系统主题切换
- themes.xml：加 `android:windowTranslucentNavigation=true` 让小白条透明
- AndroidManifest.xml：configChanges 加 `uiMode` 防止重建

### 关键 API
- `View.SYSTEM_UI_FLAG_LIGHT_STATUS_BAR` → 深色图标（浅色主题用）
- `View.SYSTEM_UI_FLAG_LIGHT_NAVIGATION_BAR` → API 26+
- `Configuration.UI_MODE_NIGHT_MASK` → 检测系统暗色模式
- `windowTranslucentNavigation` → 让内容绘制到导航栏后面

## 状态栏深色模式继续适配 v12 (18:22-18:31)

### 用户反馈 v11
- 小白条完美适配了 ✅
- 但状态栏区域没有适配深色模式 ❌

### v12 方案
- 放弃 `fitsSystemWindows(true)` + `setStatusBarColor` 方案
- 改用 `fitsSystemWindows(false)` → 内容绘制到系统栏后面
- `setStatusBarColor(TRANSPARENT)` → app 自身背景色透出
- JS 注入 `.hdr{padding-top: statusBarHeight}` 手动补偿
- `SYSTEM_UI_FLAG_LIGHT_STATUS_BAR` / `LIGHT_NAVIGATION_BAR` 切换图标颜色
- `onConfigurationChanged()` 监听系统主题切换

### 关键尝试
- `android:Theme.Material.DayNight.NoActionBar` **不存在**（平台层没有这个主题）
  - DayNight 需要 AppCompat 库，平台只有 `Theme.Material.Light.NoActionBar`
- `setStatusBarColor(Color.TRANSPARENT)` + `FLAG_DRAWS_SYSTEM_BAR_BACKGROUNDS`
  - 理论上 app 内容透到状态栏区域，深色模式时 .hdr 的深色背景自然显示
- themes.xml：`windowTranslucentStatus + windowTranslucentNavigation`

### 状态栏适配最终方案总结
| 版本 | 策略 | 效果 |
|------|------|------|
| v10 | fitsSystemWindows(true) + setStatusBarColor(#F2F2F7) | 位置对，浅色模式正常 |
| v11 | fitsSystemWindows(true) + 动态 setStatusBarColor | 深色模式状态栏颜色不生效 |
| v12 | fitsSystemWindows(false) + TRANSPARENT + JS padding | 让 app 背景透出，深/浅色自动适配 |

### GitHub API 工作流文件限制
- `.github/workflows/` 下的文件 Contents API 对 PUT 返回 400/404
- 必须手动在 GitHub 网页上修改 workflow 文件
- 其他源码文件可以通过 API 正常推送

### 构建签名流水线
```
修改源码 → API push → GitHub Actions (~60s)
→ 下载 artifact → zipalign → apksigner → 发送
```
- 工具位置：/tmp/local_jdk (JDK 17), /tmp/local_sdk/build-tools/34.0.0
- 签名命令：zipalign -f -v 4 aligned.apk → apksigner sign --ks ks
- 小米 14 刘海屏状态栏高度 134px，导航栏 48px

## v12→v13: 回退布局 + 小米私有 API (18:37-18:45)

### v12 问题
1. fitsSystemWindows(false) 导致布局再次错位 — 侧边栏文字太靠上、搜索栏位置错
2. 小米14 状态栏背景变深色但图标（时间、电量）没有变白色

### v13 修复
- 回退 fitsSystemWindows(true) 恢复正确布局（v10 方案）
- 固体色状态栏（深色 #1C1C1E / 浅色 #F2F2F7）
- 新增小米私有 API: `Window.setStatusBarDarkMode(boolean)` 通过反射调用
- 小米/MIUI/HyperOS 有自己独立的状态栏图标颜色控制 API，标准 Android `SYSTEM_UI_FLAG_LIGHT_STATUS_BAR` 不一定生效

## 侧边栏+图标+深色模式 v13 (18:43-18:54)

### 用户截图分析（v12）
- 侧边栏 "提示词盒子" 和文件夹区域太靠上（应该下移约50px）
- 状态栏背景变深了但图标（时间）没有变白色 — 小米 HyperOS 不响应标准 API

### 修复
- `.sb-hd` 注入 `padding-top:max(50px, statusBarHeight)` — 侧边栏是 position:fixed，不受 fitsSystemWindows 影响
- 小米私有 API：`Window.class.getMethod("setStatusBarDarkMode", boolean.class)` 反射调用
- 状态栏仍用固体色 `#1C1C1E`，配合小米 API 控制图标颜色

### App 图标
- 用户提供 2048x2048 PNG（紫蓝渐变圈 + 深蓝黑背景）
- 用 Pillow 生成 5 种密度：mdpi(48) hdpi(72) xhdpi(96) xxhdpi(144) xxxhdpi(192)
- 放在 app/src/main/res/mipmap-*/ic_launcher.png
- GitHub Actions workflow 的 `copy_resources()` 会自动将 res/mipmap-* 覆盖到构建产物

### 版本变迁
- v10: fitsSystemWindows(true) + #F2F2F7 状态栏 → 布局完美，浅色模式OK
- v11: 加深色模式检测 setStatusBarColor → 小米设备不生效
- v12: fitsSystemWindows(false) + TRANSPARENT → 布局崩了
- v13: 回到 fitsSystemWindows(true) + 固体色 + 小米私有 API → 待验证

## 提示词盒子 v14: Action Sheet + 更多选项模态框 (18:55-19:01)

### 用户需求
1. 卡片操作（选择文件夹、长按标签、移动到文件夹、删除等）需要统一模态框
2. 侧边栏"更多选项"点击后打开全新模态框页面
3. 模态框底部加作者信息：@皮皮灭与由mimo-v2-pro驱动的openclaw🦞共同开发

### 实现
#### Action Sheet（卡片操作）
- iOS 风格底部弹出操作菜单（毛玻璃 + 圆角）
- CSS: `.as-ol` 背景遮罩 + `.as` 底部弹出 + `.as-item` 操作项
- 点击/长按卡片都弹出 Action Sheet（替代直接编辑和直接多选）
- 操作项：复制、编辑、移动到文件夹、编辑标签、收藏、置顶、分享、多选模式、删除
- "移动到文件夹"是子页面：在 Action Sheet 内替换内容（showMoveModal）
- "编辑标签"是子页面：显示已有标签 + 添加新标签的输入框（auto focus）
- 回收站卡片显示：恢复 / 永久删除

#### 更多选项模态框
- 同样底部弹出的 action sheet 风格
- 包含原来的5个功能 + 作者信息区
- 作者信息：`.mopt-author` 区域，居中显示

### 文件
- index.html: ~71KB / ~710行
- 同步更新了 prompt-box-app/index.html (PWA 版)

### 技术要点
- GitHub API 对 70KB+ 文件推送需要 60s 超时（curl -m 60）
- Action Sheet 的子页面（移动/标签）在同一个 DOM 内替换内容，不需要新的 modal

## v14.1 & v14.2 交互重构 (19:40-20:05)

### v14.1 侧边栏微调
- 顶部区域 padding-top 20→5px（上移15px）
- 底部加 margin-bottom: 10px（上移10px）
- 已推送并签名发出 APK

### v14.2 交互重构
- **点击→编辑，长按→Action Sheet**（之前是反过来的）
- **多选模式重做**：
  - 底部弹出模态框，4宫格按钮（加标签/移动/导出/删除）
  - 底部取消按钮，风格与 Action Sheet 统一
  - 加标签：用模态框替代 prompt()，支持点击已有标签快速填入
  - 移动到文件夹：用 Action Sheet 点选替代 prompt()
- **APK 图标修复**：AndroidManifest.xml 从 `@drawable/ic_launcher` 改为 `@mipmap/ic_launcher`

### 技术要点
- batch-bar 从固定底部改为底部弹出动画（transform: translateY）
- batch-tag 用内嵌 HTML+input 替代系统 prompt()
- batch-move 用 as-item 列表替代 prompt() 输入序号
- GitHub Actions workflow 只触发 main 分支，推飞书 Git 不会触发，需要通过 GitHub API 推文件到 GitHub 才能触发构建
- GitHub API 推大文件（70KB+ index.html）需要 60s 超时

### 关于双 Git 仓库的问题
- 飞书 Git：apaas-force-git.feishu.cn（代码存储，自动构建不触发）
- GitHub：Tinyyysky/prompt_box（Actions 构建触发）
- 需要两个都推才能保持同步并触发构建

## v14.3 Bug修复 (20:06-20:12)

### 发现的Bug
1. batch-bar 默认显示（不该一直显示）→ `@keyframes batchIn` 动画覆盖了 `translateY(100%)` 隐藏效果
2. 批量加标签/移动点击无反应 → 用了 `asOvl` 但实际 ID 是 `asOL`
3. 批量删除用系统 `confirm()` → 改为模态框
4. 新建文件夹用系统 `prompt()` → 改为模态框（含8色选色器）
5. 删除文件夹用系统 `confirm()` → 改为模态框

### 修复方案
- batch-bar CSS: `display:none` + `.open` 时 `display:block`（替代 transform 动画隐藏）
- 所有 modal 引用统一用 `asOL` + `as` + `asBody`
- 新建文件夹加了颜色选择器（圆形色块点击选色）
- `delFolder` 用模态框显示文件夹名确认

### 踩坑
- Python heredoc 中用 `\\'` 转义引号在 JS 字符串中需要注意
- GitHub API 推 106KB base64 文件需要 120s 超时（之前 60s 不够）
- 文件中的中文字符是实际字符，不是 unicode escape，替换时要注意匹配方式

## v14.4 Bug修复 (21:42-21:50)

### 修复内容
1. **多选取消按钮重复** — batchTag/batchMove/batchDel 打开模态框时隐藏 batch-bar，closeAS 时恢复
2. **localStorage 存储溢出** — 图片压缩方案：1200px max + JPEG 70% + 自动降质量至30%，非图片限1MB
3. 新增 `compressImg()` 函数：Canvas 绘制→toDataURL→迭代降低质量直到<300KB

## v1.0.1 修复 (22:22-22:37)

### 修复内容
1. **收藏/置顶不响应** — `this.classList.toggle('on')` 应该用 `this.classList.toggle('starred')`，但 `this` 在 onclick 中指向有问题。改为直接 `saveD();render()` 全量渲染
2. **附件无损保存** — 移除图片压缩（compressImg），恢复原图存储。localStorage 溢出时自动用 IndexedDB 转存大附件（extractBigData）
3. **版本号更新** — build.gradle: versionCode 2, versionName "1.0.1"

### IndexedDB fallback 机制
- `saveD()` 优先用 localStorage，溢出时调用 `extractBigData()`
- `extractBigData()` 扫描 >50KB 的附件 URL，存入 IndexedDB (key: `att_{promptId}_{index}`)
- 对象上标记 `_idb` / `_idb_t`，删除 url/thumb 字段以减小 localStorage 占用
- 启动时 `restoreIDB()` 从 IndexedDB 恢复图片数据

### 踩坑
- GitHub API 推 109KB 文件时 urllib SSL 报错，curl 正常。curl 是更可靠的选择
- `base64 -w0` 在 shell 中输出无换行，配合 python -c 需要注意引号转义

## v1.0.2 图片轮播 (22:37-22:55)

### 功能
- 卡片图片区域改为轮播：touch swipe + 箭头按钮
- 底部圆点指示器：当前图片 active 状态拉长高亮
- CSS 过渡 0.3s cubic-bezier(.25,.46,.45,.94)，拖拽中 transition:none
- card-gal 的 onclick="event.stopPropagation()" 防止点击图片触发编辑
- 最多10张图片，最多10个圆点

## v1.0.2 堆叠图片 (22:50-23:10)

### 功能
- 卡片图片区从轮播改为**堆叠照片效果**
- 多张图片轻微旋转错开，带阴影+白色描边
- 右下角 "N 张" 徽章（glassmorphism 风格）
- 点击堆叠 → `stackFanOut()` 扇形展开动画
- hover 时每张图独立弹性位移（cubic-bezier .34,1.56,.64,1）
- 最多显示 5 张（堆叠效果超过5张太密集）

### 技术
- 去掉了 carousel/gal-track 相关代码
- 用 CSS transform + z-index 实现堆叠层次感
- fan-out 用正弦函数计算扇形角度

## v1.0.2b 堆叠滑动 (23:18-23:25)

### 交互
- 多张图片像牌堆叠放，后排露出左边缘（clip-path inset）
- 偏移：每层 8px 水平 + 6px 垂直 + 微旋转 + 0.97 缩放
- 滑动跟手：translateX + 旋转 + opacity 渐变
- 飞出阈值 80px，飞出后 DOM insertBefore 到队尾重新 initStack
- 不够 80px 弹性回位

### 技术
- initStack() 递归调用重新定位（滑出后 insertBefore 到首位再调 initStack）
- touch + mouse 双支持
- clip-path: inset(0 0 0 Npx) 揭露后排图片左侧

## v1.0.2c 循环滑动 (23:26-23:39)

### 交互
- 堆叠显示 3 层图片，后排 clip-path 揭露左边缘
- `_cur` 记录当前索引，左滑+1，右滑-1，模运算循环
- 滑过 60px 自动飞走（translateX 500px + rotate 25deg）
- 0.3s 动画后立即可再次滑动（animating 标志控制）
- 松手不够距离弹回（0.25s cubic-bezier 回弹）
- `pointerEvents` 只对最顶层图片启用

### 技术
- initStack 递归调用处理循环后的重新排列
- touch + mouse 双支持，`_bound` 标志避免重复绑定
- 飞出后 setTimeout 300ms 后重置 transition + 重新 initStack

## v1.0.3 IndexedDB 存储升级 (23:42-23:55)

### 问题
- localStorage 5MB 上限，base64 图片几张就爆
- 用户反馈"存储不足"且附件消失

### 解决方案
- 附件改存 IndexedDB（PBDB_NAME='PromptBoxDB', store='blobs'）
- localStorage 只存元数据（{id, type, name, size}）
- 旧 base64 数据首次启动自动迁移（fetch data: URL → ArrayBuffer → IDB）
- 异步加载：先显示占位符（🖼️），加载完替换 img
- 删除提示词/附件同步清理 IDB 碎片

### 技术
- dbSet/dbGet/dbDel 封装 IDB 事务
- saveAttachment(file) → fileToArrayBuffer → dbSet
- loadAttachmentURL(att) → dbGet → blobToURL → ObjectURL
- renderAttachments 改为 async，支持新旧格式混合
- card rendering 用 data-att-id 属性异步加载图片

## v1.0.3.1 修复附件显示 (23:55-00:00)

### 问题
v1.0.3 的 IndexedDB 改造导致：
- 编辑时预览图片不显示
- 卡片上图片不显示
- 不能添加多个附件
- 附件完全看不到

### 修复思路
- 不再完全依赖 IDB 异步加载
- 编辑时用临时 data URL（内存中，不持久化）
- 持久化：大文件→IDB，小文件→localStorage base64
- 编辑加载：先尝试 IDB blob URL，fallback 到 localStorage base64
- 卡片渲染同步显示（IDB 是异步的会闪烁）

### 状态
已推送到 GitHub，等待用户验证

## v1.0.3.2 附件修复尝试 (00:12-00:22)

### 方案
- preFiles: URL.createObjectURL 生成临时 blob URL 显示
- 后台 fileToArrayBuffer → dbSet 存 IDB
- editP async, 从 IDB 加载 blob URL
- 卡片: 有 url 直接显示，无 url 异步加载

### 结果
❌ 用户反馈仍不显示

## v1.0.3.3 彻底回滚方案 (00:34-01:00)

### 问题根因
IDB 异步架构太复杂，每次修一点另一边又坏。不如回到简单方案。

### 最终方案
- **砍掉全部 IDB 代码**
- 回归 base64 存 localStorage（v1.0.2 之前的工作方案）
- 新增：图片压缩（>200KB 自动压缩到 1200px + 65% 质量 JPEG）
- compressImage() 用 canvas toBlob 压缩
- renderAttachments / editP / 卡片渲染全部回到同步代码
- 删除函数也回到同步（无 IDB 清理）

### 教训
- 简单方案出问题好修，复杂异步架构一环扣一环难调试
- IDB 方案技术上正确但实现太重，容易踩坑
- 图片压缩用 canvas toBlob 是解决 localStorage 限制的更简单路径
